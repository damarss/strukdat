#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main() {
    int a[5];
    FILE *nama;
    nama = fopen("./file/a.csv", "r");
/*    if (nama == NULL) {
        printf("Gaada filenya goblok\n");
    }
    
    fscanf(nama, "%d %d", &a[0], &a[1]);
    printf("a = %d\nb = %d\n", a[0],a[1]); */

    int i = 0;
    while (i == 0) {
        printf("Arin ");
    }
    
    return 0;
}